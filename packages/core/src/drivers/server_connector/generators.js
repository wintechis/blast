/**
 * @fileoverview Javascript generators for server conncector.
 * @author derwehr@gmail.com(Thomas Wehr)
 * @license https://www.gnu.org/licenses/agpl-3.0.de.html AGPLv3
 */

import Blockly from 'blockly';
const {JavaScript} = Blockly;

/**
 * Generates JavaScript code for the server_add_connector block.
 * @returns {String} the generated code.
 */
JavaScript['server_add_connector'] = function () {
  // this code will be overwritten when BLAST is run using the cli version.
  // being incompatible with the browser version, this has to be null here
  return null;
};

/**
 * Generates JavaScript code for the server_route block.
 * @param {Blockly.Block} block the server_route block.
 * @returns {String} the generated code.
 */
JavaScript['server_route'] = function () {
  // this code will be overwritten when BLAST is run using the cli version.
  // being incompatible with the browser version, this has to be null here
  return null;
};

JavaScript['server_response'] = function () {
  // this code will be overwritten when BLAST is run using the cli version.
  // being incompatible with the browser version, this has to be null here
  return null;
};

/**
 * Generates JavaScript code for the response block.
 * @param {Blockly.Block} block the response block.
 * @returns {String} the generated code.
 */
JavaScript['server_get_body'] = function () {
  // this code will be overwritten when BLAST is run using the cli version.
  // being incompatible with the browser version, this has to be null here
  return null;
};
