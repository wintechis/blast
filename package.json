{
    "private": true,
    "name": "blast",
    "description": "Block Applications for Things",
    "version": "0.0.1",
    "author": "Thomas Wehr",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/wintechis/blast.git"
      },
    "license": "AGPL-3.0",
    "scripts": {
        "build": "yarn workspaces run build",
        "lint": "yarn workspace @blast/core run lint",
        "test": "yarn workspaces run test",
        "install": "yarn workspace @blast/core install && yarn workspace @blast/web install",
        "serve": "yarn workspace @blast/web run serve"
    },
    "workspaces": [
        "./packages/*"
    ]
}
